export enum Roles {
  ROLE_ADMIN = 'ROLE_ADMIN',
  ROLE_USER = 'ROLE_USER',
}

export enum Status {
  ACTIVE = 'ACTIVE',
  BLOCKED = 'BLOCKED',
}

export enum MovieType {
  TwoD = '2D',
  ThreeD = '3D',
}

export enum BannerType {
  IMAGE = 'IMAGE',
  VIDEO = 'VIDEO',
}

export enum SeatType {
  Standard = 'STANDARD',
  VIP = 'VIP',
  Sweetbox = 'SWEETBOX',
}

export enum Position {
  Header = 'Home-Header',
  Siderbar = 'Home-Sidebar',
  Footer = 'Home-Footer',
  Popup = 'Home-Popup',
  Slider = 'Home-Slider',
}

export enum PromotionDiscountType {
  PERCENT = 'PERCENT',
  AMOUNT = 'AMOUNT',
}
